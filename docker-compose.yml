version: '3.8'

services:
  typst-api:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    volumes:
      # Mount templates directory for easy updates without rebuilding
      - ./templates:/app/templates:ro
    restart: unless-stopped
